# Этап 13 — общая проверка CRM

Дата: 14.09.2026. Ветка для проверки: `codex/crm-stage-13`. PR: #373 (draft, base main). База сборки — этап 12, `7d226bb51ee063a27dbd30ecb5f02d6f260fcb59`. Автоматического слияния или переключения приложения не выполняли.

## Результат аудита

**Полное соответствие требованиям пока не подтверждено.** Код и тесты показывают работающую основу этапов 01–11, но обнаружены сквозные ошибки и незавершённые сценарии. Этап 12 — проект подключения Avito, не действующая интеграция. Этап 13 остаётся открытым до проверки переключённого приложения и закрытия перечисленных ниже расхождений. Зелёный CI не означает, что все бизнес-сценарии завершены.

Аудит опирается на серверные запросы/транзакции, обработчики API, состояния worker, миграции, компоненты UI и тестовые сценарии. Отчёты прошлых этапов использованы только как перечень заявлений для проверки. Контрольный источник требований — исходный пакет `ficusin-crm-agent-prompts.md` и `requirements.md`; поздние решения владельца имеют приоритет.

## Исправлено в общей ветке

1. `CreateShipmentOffer` вычитает позиции всех оплаченных/исполняемых/завершённых отправок. После перехода paid → shipped позиция больше не становится доступной к повторному предложению.
2. `reserveOfferAndCreatePayment` закрывает курсор остатков перед изменением тех же данных в транзакции. Старый тест использовал заранее зарезервированный товар и не обнаруживал `conn busy`. Теперь тест начинает с нулевого резерва.
3. Неопределённая ошибка создания платежа сохраняет pending и ключ идемпотентности. Повтор не создаёт новый независимый платёж и не освобождает товар до выяснения результата.
4. Повтор успешного платёжного уведомления не переводит отправленное предложение обратно в paid. Pending-платежи частичных отправок с известным ID провайдера проверяются и после двух часов.
5. `RefreshSiteSales` учитывает фактически оплаченный состав частичных отправок по их снимкам, не весь неоплаченный родительский предзаказ. Повтор синхронизации не удваивает продажи.
6. Упаковка не начисляется на самовывоз, неоплаченные позиции, техническую строку доставки и товар без подтверждённой принадлежности к растениям. Для сайта требуется известная отправка СДЭК. Ставка уже созданного снимка сохраняется. Исправление схемы отдельной миграцией, исторический файл этапа 11 не переписан.
7. P&L использует московский календарный день, как реестр продаж. Банковская оплата поставщику не дублирует себестоимость реализованного товара.
8. Возврат без подтверждённой исходной продажи не восстанавливает себестоимость и не создаёт прибыль из ручной записи. Неразобранные банковские строки и неподтверждённая полнота отчётов МП делают P&L предварительным. Одна комиссия не означает закрытие всего периода.
9. JSON `null` в налоге и счёте поставщика отображается как неизвестное значение, а не 0. Типы интерфейса согласованы с API.
10. В описании Avito исправлено неверное заявление о поддержке канала таблицей sales_events: действующий CHECK допускает четыре канала, расширение схемы ещё требуется.

## Матрица требования → код → тест → доказательство/ограничение

Пути относительно корня репозитория. Имена тестов позволяют повторить проверку, а не только прочитать обещание. Все `*Live*` CRM-тесты требуют **изолированной** базы и последовательных seed/stage шагов workflow; на боевой базе их запускать нельзя.

| Этап / требование | Исполняемый код | Проверка | Статус и граница доказательства |
|---|---|---|---|
| 01: оболочка, владелец/менеджер, клиенты readonly | `frontend/src/AdminPage.tsx`, `backend/internal/admin/admin.go`, `httpapi/admin_field_permissions.go` | `TestCRMManagerDeniedRoutes`, `TestCRMManagerProductPatchRejectsEntireMixedRequest`, `TestCRMManagerVariantMixedPatchRejected`; e2e owner/manager | Проверяется API-запрет смешанного PATCH целиком, не только скрытые кнопки; визуальная приёмка на сайте впереди |
| 02: общая синхронизация и ручная кнопка без гонки | `procurement/postgres_integration_sync.go`, `catalog_worker.go`, `worker.go`, `postgres_actions.go` | `TestIntegrationCoordinatorLive`; тесты интеграций и saby scripts в Verify | Lease/token, coalescing, cooldown проверяются на PostgreSQL. Реальные лимиты/429 и отсутствие стороннего расписания на сервере требуют проверки конфигурации |
| 03: основная карточка, связи, категории, подборки/обложки | `admin/catalogue.go`, `admin/permissions.go`, `httpapi/admin_field_permissions.go`, `frontend/src/AdminCatalog.tsx`, `AdminPage.tsx` | `TestStage03CatalogueSafetyOnLiveDatabase`, `TestCRMContentEditPreservesGallery`; desktop/mobile e2e | Конфликт master identity, сохранность данных и ограничение менеджера. Реальные фото/полнота связей проверяются на сайте |
| 04: единые продажи, возвраты, дубли и обзор | `procurement/sales_events.go`, `postgres_catalogue.go`, `analytics/analytics.go` | `TestStage04SalesLedgerOnLiveDatabase`, `TestStage04SalesSummaryOnLiveDatabase`, `TestStage13PartialSalesDoNotCountUnpaidPlants` | Повторный импорт, неоплаченные позиции и сверка каналов. Avito ещё не импортируется; точность cross-source идентификаторов нужно сверять с живыми отчётами |
| 05: рекомендации, лист ожидания, заявки/план | `procurement/recommendations.go`, `postgres_plan.go`, `postgres.go` | `TestStage05PartialRequestAllocationOnLiveDatabase`, recommendation/plan calculator tests; e2e | История отдельно от горизонта закупки, неизвестный остаток не считается нулём, кратность и заявки. Наличие у поставщика проверяется вручную |
| 06: неизменный план и ревизии инвойса | `procurement/postgres.go:ImportDocument/loadOrderValidation` | `TestStage06PlanAndInvoiceRevisionsOnLiveDatabase`; e2e четыре состояния сверки | Hash-дедупликация документа, запрет тихого переноса, блокеры арифметики/сопоставления. Новый реальный инвойс №18 не предоставлен и не проверен |
| 07: расчёт, цены, черновик поступления, история стоимости | `procurement/calculator.go`, `postgres_actions.go`, `sales_events.go` | `TestStage07PostedReceiptAndDatedCostsOnLiveDatabase`, calculator tests | Проведение должно быть подтверждено СБИС; устаревший finisher отклоняется; историческая стоимость сохранена. Фактическое создание документа и цены в МП не запускались |
| 08: частичная отправка, письмо/48ч, оплата/коробки | `admin/shipment_offers.go`, `order/letters.go`, `payment/adjustments.go`, `payment/reconcile.go`, `order/shipping.go` | `TestStage08OfferSnapshotsSelectedQuantitiesAndBoxes`, `TestStage08NotificationStartsOneStableFortyEightHourWindow`, `TestStage08FailedSMTPDoesNotStartExpiry`, `TestStage08ShipmentPaymentIsIdempotentAndHasExactReceipt` | Регрессии резерва/повтора оплаты исправлены; **сценарий целиком не принят**, см. R1–R4 |
| 09: отдельная запись на растение, состояние, поступление без автопроведения | `admin/marketplace_returns.go`, `httpapi/admin_marketplace_returns.go`, `procurement/postgres_actions.go` | `TestStage09PlantReturnsAreIdempotentAndDoNotChangeStock`; e2e менеджера/владельца | Локальный остаток не меняется вручную; история и снимок стоимости. Живой документ СБИС и связанные комиссии требуют сверки |
| 10: три банка, исходный файл, дедупликация/классификация/касса | `admin/finance.go`, `admin/finance_parser.go`, `frontend/src/AdminFinance.tsx` | `TestFinanceXLSXIgnoresBrokenDimensionAndReadsAllRows`, `TestFinanceDedupeUsesMoreThanDocumentNumber`, `TestFinanceClassificationKeepsTransfersOutOfPnL`, `TestShortSberPDFNeedsReview` | Синтетические форматы проверены; сверка реальных начальных/конечных остатков и редактор правил ещё не завершены |
| 11: P&L, историческая стоимость, расходы, EUR-счёт | `admin/finance_pnl.go`, миграции stage11/stage13, `AdminFinanceStage11.tsx` | `TestStage11PnLAndSupplierAccountOnLiveDatabase`, `TestStage13PnLUsesMoscowBusinessDay`; e2e null-значений | Синтетическая арифметика с возвратом, поставщиком, налогом, упаковкой и неизвестной стоимостью. Полнота реальных отчётов не доказана; баланс бизнеса не реализован |
| 12: Avito | `docs/crm/stage-12.md`, отображение канала без данных | Проверка кода ограничения sales_events и отсутствия адаптера | Исследовательский этап; API/tарифные права не подтверждены. Ни публикаций, ни автоснятия нет |
| 13: сборка/регрессии/приёмка | `.github/workflows/crm-acceptance.yml`, `e2e/verify-crm-acceptance.mjs`, новые stage13 tests | Verify + CRM acceptance + CodeQL на точном SHA ветки | CI — изолированная база, desktop 1440/mobile 390. Проверка переключённого приложения ещё впереди |

## Открытые расхождения — не скрывать за зелёным CI

| ID | Наблюдение по коду | Что требуется до приёмки сценария |
|---|---|---|
| R2 | `AdminShipmentOfferBuilder` передаёт стоимость/код тарифа от клиента, создаёт только отдельные коробки; сервер проверяет наличие тарифа, но не подтверждает новую котировку СДЭК | Полный редактор объединения коробок и серверная котировка по точному составу/адресу; пересчёт при изменении и запрет оплаты до подтверждения |
| R3 | Ошибка создания платежа теперь сохраняет ключ, но при отсутствии provider_payment_id после таймаута worker не может самостоятельно найти платёж; после 48ч клиентская попытка отклоняется | Долговечное разрешение unknown по тому же ключу, безопасная сверка/операторский экран. Не отменять неизвестную оплату и не освобождать резерв вслепую |
| R4 | `order/shipping.go` записывает shipped после создания заявки СДЭК; факт создания заявки не доказывает передачу посылки. Ошибка CreateOrder переводит заявку на retry | Различить «заявка создана» и физическую передачу, подтвердить идемпотентность/сверку неоднозначного CreateOrder. До этого норматив упаковки сайта ориентируется на состояние CRM, а не скан перевозчика |
| R5 | Упаковка группируется P&L по дате продажи; если отправка в другом периоде, отдельной даты признания упаковки нет. Товары ограничены 100 строками; каналы формируются из продаж периода | Отдельная дата отправки/расхода, пагинация, каналы/товары только с возвратами или расходами за период и сверка сумм с итогом |
| R6 | Нет закрытия периода финансовых отчётов площадок; банк/МП могут содержать одну и ту же комиссию | Подтверждённый импорт и связывание первичных строк расходов, контроль полноты и межисточниковых дублей. Сейчас отчёт явно предварительный |
| R7 | Сверка конечного остатка выписки, правила классификации пользователя, автоматическая касса из СБИС не реализованы полностью | Проверить реальные файлы приватно, добавить контроль оборотов; для кассы сначала получить надёжный способ оплаты продажи |
| R8 | Себестоимость становится текущей при подтверждённом поступлении (`postgres_actions.go`), тогда как владелец знает её при окончательном инвойсе | Разделить фиксацию окончательного расчёта инвойса и дату применения текущей стоимости. Согласовать/проверить момент применения, не переписывать исторические продажи |
| R9 | Реальная цепочка сайт → СБИС → каналы, актуальность входящих остатков и документов не доказана тестами с подменёнными провайдерами | Проверить конфигурацию и журналы после переключения; боевую операцию выполнять только как согласованную реальную операцию, не многократно ради теста |

## Проверки и материалы

Workflow CRM acceptance запускает миграции и все этапы по порядку на отдельном PostgreSQL без внешних ключей. Платёжный провайдер в тестах подменён; SMTP подменён; реальные цены, кассовые чеки и поступления не отправляются.

Новые регрессии проверяют: резерв предзаказа с нуля; неопределённый ответ оплаты с сохранением ключа; повтор webhook после shipped; попытку повторной отправки уже оплаченной единицы; только оплаченные растения в реестре продаж; самовывоз без упаковки; стабильную упаковку после повторной синхронизации; московскую границу суток; ручной несвязанный возврат; покупку у поставщика без повторного COGS; null в UI.

Итоговый статус запусков смотреть на текущем HEAD PR #373. Нельзя переносить зелёный статус старого SHA на новый. Скриншоты в артефакте `crm-stage-13-acceptance`; это проверка тестового интерфейса, не снимки боевого магазина. Исходные согласованные макеты остаются эталоном; новых макетов на этом этапе не создавали.

Инструкция проверки/отката — `stage-13-handoff.md`. Защищённые закупки П3-12963 и №18 не редактировались, инвойсы и заказы не удалялись. Ни один тест из workflow не предназначен для боевой базы.

## Зафиксированный прогон

Кодовый SHA `fe756b8209b8f296570c72418c430bc0838249ca`:

- Verify #1524, run 34806995037 — success (backend tests/vet/build, frontend lint/build/budget, миграции, Saby tests, Docker/runtime, layout).
- CRM acceptance #42, run 34806995054 — success: все этапы PostgreSQL, новые stage13 регрессии и desktop/mobile API/UI, без пропущенного обязательного шага.
- CodeQL #117, run 34806995006 — success.
- Локально `npm run build`, `npm run lint`, `git diff --check` — успешно.
- После полного чтения git-истории `merge-base --is-ancestor` подтвердил включение **каждой** ветки codex/crm-stage-01 … codex/crm-stage-12 в общую ветку.

Следующий коммит добавляет только этот отчёт и handoff; исполняемый код и тесты не меняет. Подтверждение сайта отсутствует: ожидается переключение приложения владельцем. R1–R9 остаются открытыми, результаты этого прогона их не закрывают.


## Проверка переключённого приложения

Проведена авторизованная проверка интерфейса владельца на ветке этапа 13. Просмотрены обзор, аналитика, маркетплейсы, закупки и рекомендации, карточка защищённой закупки №18, каталог, категории, подборки, заказы, клиенты, возвраты, финансы, P&L, счёт поставщика и настройки. Боевые данные не изменялись: не запускались синхронизации, платежи, письма, поступления, изменение цен и удаление записей.

Подтверждено:

- защищённая закупка №18 сохранена; инвойс отсутствует, расчёт заблокирован до его загрузки;
- форма подборки требует обложку;
- P&L помечает неполные данные предварительными;
- настройки записи остатков в СБИС и создания заказов СДЭК выключены;
- Wildberries возвращает ограничение 429, а очередь показывает паузу площадки;
- при первой живой проверке линия продаж СБИС была отключена, хотя карточка интеграции определяла состояние только по наличию учётных данных.

По результатам проверки исправлен интерфейс: обзор явно показывает продажи всех каналов; недоступные источники не скрывают сохранённую сумму; карточка интеграции показывает частичное подключение; время очереди фиксировано в часовом поясе Москвы; способы доставки переведены на русский; закрытые заказы доступны только для просмотра; без инвойса число телег больше не приписывается парсеру. Эти проверки добавлены в e2e-приёмку.

Граница проверки: SHA развёрнутого backend из интерфейса определить нельзя; подтверждён только frontend bundle выбранной ветки. Живой сеанс менеджера и мобильный экран не проверялись вручную, они покрыты изолированным e2e workflow. После проверки добавлен прямой источник продаж Saby Retail в общий worker, а автоматический внешний GitHub-запуск отключён. Код и синтетический ответ API проверены, но полноту реальных чеков ещё нужно сверить после развёртывания. Ограничение Wildberries остаётся внешней эксплуатационной задачей; зелёный UI-тест его не снимает.


## Продажи СБИС после живой проверки

Коммит этапа 13 добавляет `integration.SalesExecutor`: Saby и Ozon проходят через `SalesWorker`, lease и общий ограничитель запросов. Saby Retail `/retail/order/list` читается постранично; сохраняются стабильные идентификаторы документа и строки, возвраты и суммы. Повтор страницы останавливает импорт с ошибкой, поэтому неполный снимок не подменяет последний успешный. Московская дата используется для окна синхронизации. Почасовой GitHub workflow больше не запускается автоматически и не конкурирует с приложением.

Автотест проверяет авторизацию Saby, параметры точки и периода, продажу, возврат и стабильные ключи строк. Это не доказывает полноту ответа боевого кабинета: после развёртывания требуется сверить количество и суммы с журналом Saby за один закрытый день.


## Актуальная цена частичной отправки

При создании предложения каждая позиция получает текущую цену варианта и действующую розничную скидку клиента. Цена первоначального предзаказа сохраняется рядом для объяснения изменения. После создания предложения обе суммы являются снимком: последующее изменение карточки не меняет уже предложенную или оплаченную отправку. Если актуальная цена варианта недоступна, используется цена исходного заказа.

Live PostgreSQL test проверяет изменение базовой цены, скидку клиента, округление, исходную цену и неизменность созданного снимка. Реальная платёжная операция не выполняется.
